- defaults:
    link:
        create: true
        relink: true
        force: true

- clean: ['~']

- shell:
    - 
        command: ./install-oh-my-zsh
        description: Running zsh scripts
        stdout: true
        stderr: true
        quiet: true

- link:
    ~/.zshrc: 
    ~/.gitconfig: 
