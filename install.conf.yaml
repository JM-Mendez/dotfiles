- defaults:
    link:
        create: true
        relink: true
        force: true

- clean: ['~']
        
- shell:
    - [curl -fsSL https://fnm.vercel.app/install | bash, Installing fnm (fast node manager)]
        
- shell:
    - 
        command: bash ./install-oh-my-zsh
        description: Running zsh scripts
        stdout: true
        stderr: true
        quiet: true

- link:
    ~/.zshrc: 
    ~/.gitconfig: 
